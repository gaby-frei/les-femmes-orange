{
  "name": "les-femmes-orange",
  "version": "0.0.0",
  "private": true,
  "description": "Community site gated by a Nostr web-of-trust membership list.",
  "scripts": {
    "start": "node server.js",
    "test": "playwright test",
    "test:playwright": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "nostr-tools": "^2.10.0"
  }
}
