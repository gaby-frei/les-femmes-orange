{
  "name": "les-femmes-orange",
  "version": "0.0.0",
  "private": true,
  "description": "Community site gated by a Nostr web-of-trust membership list.",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.js && playwright test",
    "test:unit": "node --test test/*.test.js",
    "test:playwright": "playwright test",
    "eval:relevance": "node eval/relevance.eval.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "nostr-tools": "^2.10.0"
  }
}
